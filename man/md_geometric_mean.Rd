% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_geometric_mean.r
\name{md_geometric_mean}
\alias{md_geometric_mean}
\title{Calculate Geometric Mean}
\usage{
md_geometric_mean(x, na.rm = TRUE)
}
\arguments{
\item{x}{Numeric vector of values to average}

\item{na.rm}{Logical, whether to remove NA values (default: TRUE)}
}
\value{
Numeric value representing the geometric mean
}
\description{
Computes geometric mean with proper handling of negative values and zeroes.
Uses log transformation for numerical stability and handles edge cases gracefully.
}
\details{
The function:
\itemize{
\item Handles NA values by removing them (if na.rm = TRUE)
\item Uses log transformation for computation: exp(mean(log(1 + x))) - 1
\item Returns NA for empty or all-NA inputs
\item Adjusts values by +1 before calculation to handle negative values
\item Suitable for growth rates and percentage changes
}
}
\examples{
\dontrun{
# Growth rates as decimals
growth_rates <- c(0.05, 0.03, -0.02, 0.08)
md_geometric_mean(growth_rates)

# Handle missing values
with_na <- c(0.05, NA, 0.03, 0.08)
md_geometric_mean(with_na, na.rm = TRUE)
}

}
