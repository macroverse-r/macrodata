% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/md_info.r
\name{in_info_special}
\alias{in_info_special}
\title{Display Special Data Information}
\usage{
in_info_special(
  special,
  frequency = NULL,
  origin = NULL,
  search = NULL,
  color = TRUE,
  colors_def = NULL,
  print = TRUE
)
}
\arguments{
\item{special}{Character string specifying the special data type (currently only "BOP" is supported)}

\item{frequency}{Character, either "Q", "Y", or NULL (ignored when special is used, triggers warning)}

\item{origin}{Character vector of data origins (ignored when special is used, triggers warning)}

\item{search}{Character vector of search terms to filter data (searches across all columns)}

\item{color}{Logical, whether to use color highlighting (default = TRUE)}

\item{colors_def}{List of ANSI color codes used for highlighting search matches}

\item{print}{Logical, whether to print the output to console (default = TRUE)}
}
\value{
Invisibly returns the displayed data frame
}
\description{
Internal function to handle special data display cases in wp_info. Currently supports
Balance of Payments (BOP) data display with search and highlighting capabilities.
}
\details{
This function provides specialized display formatting for different types of data.
For BOP data:
\itemize{
\item Displays data in a formatted table with aligned columns
\item Supports text wrapping for long entries
\item Implements search across all columns
\item Provides color highlighting for search matches
\item Automatically adjusts to terminal width
}

The function will issue warnings if:
\itemize{
\item Terminal width is too narrow for proper display
\item frequency or origin parameters are provided (as they are ignored)
}

The function's output can be controlled via the print parameter:
\itemize{
\item When print=TRUE (default), displays formatted output to console
\item When print=FALSE, silently returns the filtered data
}
}
\examples{
\dontrun{
# These are called internally by wp_info:
in_info_special("BOP", search = "Export")
in_info_special("BOP", search = c("Export", "Import"), color = TRUE)
}

}
\seealso{
\code{\link{wp_info}}
}
\keyword{internal}
